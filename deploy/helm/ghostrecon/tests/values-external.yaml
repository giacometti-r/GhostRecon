postgresql:
  enabled: false

redis:
  enabled: false

secretEnv:
  GHOSTRECON_DATABASE_URL: postgresql+asyncpg://ghostrecon:external-password@postgresql.example.internal:5432/ghostrecon
  GHOSTRECON_REDIS_URL: rediss://:external-password@redis.example.internal:6380/0?ssl_cert_reqs=required
